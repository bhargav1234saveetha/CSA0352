#include <stdio.h>

int main() {
    int n, x;

    printf("How many numbers? ");
    scanf("%d", &n);

    while(n--) {
        scanf("%d", &x);
        printf("%d is %s\n", x, (x % 2 == 0) ? "Even" : "Odd");
    }

    return 0;
}
