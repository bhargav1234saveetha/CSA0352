#include <stdio.h>

int f(int n) {
    return (n <= 1) ? n : f(n-1) + f(n-2);
}

int main() {
    int n, i;
    scanf("%d", &n);
    for(i = 0; i < n; i++)
        printf("%d ", f(i));
    return 0;
}
